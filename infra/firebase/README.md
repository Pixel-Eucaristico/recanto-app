# Firebase IaC

Arquivos ativos hoje:

- `firebase.json`
- `firestore.rules`
- `firestore.indexes.json`
- `database.rules.json`
- `scripts/deploy-firebase.mjs`

Proximo passo: mover ou sincronizar esses arquivos para esta pasta e atualizar os scripts
`firebase:*` para usar caminhos declarativos em `infra/firebase`.
